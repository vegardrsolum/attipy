[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "attitudex"
authors = [
  { name="Vegard R. Solum", email="vegard.rorvik.solum@gmail.com" },
]
description = "Attitude and sensor fusion utilities"
dynamic = ["version"]
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.urls]
Homepage = "https://github.com/vegardrsolum/attitudex"
Issues = "https://github.com/vegardrsolum/attitudex/issues"

[tool.hatch.version]
path = "src/smsfusion/__about__.py"
